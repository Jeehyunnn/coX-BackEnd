spring:
  application:
    name: coX

  datasource:
    url: jdbc:mariadb://localhost:3306/coX
    username: admin
    password: 07080708
    driver-class-name: org.mariadb.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

server:
  port: 7878  # 7878? ??? ??
